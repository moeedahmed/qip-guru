[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "qip-guru-kit"
version = "0.1.0rc1"
description = "QIP Guru Kit: local-first healthcare QI scaffold, source profiles, static skill guides, and deterministic de-id helpers."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
  { name = "Moeed Ahmed" }
]
keywords = ["quality-improvement", "clinical-audit", "patient-safety", "healthcare", "qip"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Healthcare Industry",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Scientific/Engineering :: Medical Science Apps."
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
qip = "qip:main"

[tool.setuptools]
py-modules = ["qip"]

[tool.setuptools.packages.find]
include = ["qipkit*"]

[tool.setuptools.data-files]
"qip-guru-kit/checklists" = ["checklists/*.md"]
"qip-guru-kit/standards" = ["standards/*.json"]
"qip-guru-kit/templates" = ["templates/*.md"]
